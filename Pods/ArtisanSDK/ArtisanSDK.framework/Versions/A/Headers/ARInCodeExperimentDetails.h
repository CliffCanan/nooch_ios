//
//  ARInCodeExperimentDetails.h
//  ARUXFLIP
//
//  Copyright (c) 2014 Artisan Mobile. All rights reserved.
//
//

#import <ArtisanSDK/ArtisanSDK.h>
#import <ArtisanSDK/ARExperiment.h>
#import "ARExperimentDetails.h"

/**
 * An object containing useful information about an in-code experiment
 * These are retrieved via the `ARExperimentManager#getInCodeExperimentDetails` method.
 **/
@interface ARInCodeExperimentDetails : NSObject

/**
 * The name of the experiment. This name is set up when the experiment was started on Artisan Tools.
 *
 * @warning This will be nil if the experiment isn't running
 */
@property (nonatomic, readonly) NSString *experimentName;

/**
 * The id of the experiment. This is generated by Artisan when the experiment is started and is unique
 * to all experiments.
 *
 * @warning This will be nil if the experiment isn't running
 */
@property (nonatomic, readonly) NSString *experimentId;

/**
 * The description of the experiment.
 */
@property (nonatomic, readonly) NSString *experimentDescription;

/**
 * The in-code name used to register this experiment.
 */
@property (nonatomic, readonly) NSString *inCodeName;

/**
 * The name of the default variation for the experiment. If no variation has been
 * explicitly defined as the default, it's assumed to be the first variation
 * that was added.
 */
@property (nonatomic, readonly) NSString *defaultVariation;

/**
 * The current variation id for the experiment.
 *
 * @warning This will be nil if the experiment isn't running
 */
@property (nonatomic, readonly) NSString *currentVariationId;

/**
 * The current variation name for the experiment.
 *
 * @warning If this experiment is not running the current variation name will be
 * the same as the default variation or if the experiment was published from Artisan Tools 
 * then it will be the published variation.
 */
@property (nonatomic, readonly) NSString *currentVariationName;

/**
 * The start date of this in-code experiment. This is set automatically by Artisan.
 *
 * @warning This will be nil if the experiment isn't running
 */
@property (nonatomic, readonly) NSDate *startDate;

/**
 * The end date of this in-code experiment. This is set automatically by Artisan.
 *
 * @warning This will be nil if the experiment isn't running
 */
@property (nonatomic, readonly) NSDate *endDate;

/**
 * Whether this experiment is currently running in Artisan Tools.
 * 
 * @warning This flag will be true if you've used the `ARExperimentManager#startExperiment:variantName:`
 * method but all of the web side experiment details (currentVariationId, currentVariationName, experimentId ect.)
 * will be nil since this experiment wasn't kicked off from the web.
 */
@property (nonatomic, readonly) BOOL isRunning;

/**
 * This is the init to build the ARInCodeExperimentDetails object.
 * You won't need to use this.
 */
-(id)initWithExperiment:(ARExperiment *)experiment;

/**
 * This is the init to build the ARInCodeExperimentDetails object.
 * You won't need to use this.
 */
-(id)initWithExperiment:(ARExperiment *)experiment dictionary:(NSDictionary *)detailsDictionary;

@end
